# Production / Vercel: set to your deployed API origin (no trailing slash), then rebuild the client.
# Example: https://your-backend.vercel.app
# Vercel → Project (frontend) → Settings → Environment Variables → Production (and Preview).
VITE_API_URL=
